% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-chord.R
\name{plot_chord_ror}
\alias{plot_chord_ror}
\title{Plot a chord diagram of modification rewiring between conditions.}
\usage{
plot_chord_ror(
  ror_data,
  ror_cutoff = 0.5,
  gained_color = "#D55E00",
  lost_color = "#0072B2",
  sprinzl_coords = NULL,
  mods = NULL,
  title = NULL,
  transparency = 0.4
)
}
\arguments{
\item{ror_data}{A tibble from \code{\link[=compute_ror]{compute_ror()}} with columns \code{pos1}, \code{pos2},
and \code{log_ror}.}

\item{ror_cutoff}{Minimum absolute value of \code{log_ror} for a chord to be
drawn. Default \code{0.5}.}

\item{gained_color}{Color for gained dependencies (positive log ROR).
Default \code{"#D55E00"} (vermillion).}

\item{lost_color}{Color for lost dependencies (negative log ROR).
Default \code{"#0072B2"} (blue).}

\item{sprinzl_coords}{An optional tibble from \code{\link[=read_sprinzl_coords]{read_sprinzl_coords()}} used
to order sectors and color by structural region.}

\item{mods}{An optional tibble of modification annotations (e.g., from
\code{\link[=fetch_modomics_mods]{fetch_modomics_mods()}}) with columns \code{pos} and \code{mod1}. When provided
along with \code{sprinzl_coords}, modification positions are highlighted
as an annotation ring.}

\item{title}{Optional plot title.}

\item{transparency}{Transparency for chord colors. Default \code{0.4}.}
}
\value{
\code{invisible(NULL)}. Called for its side effect of producing a
base graphics plot.
}
\description{
Display a circlize chord diagram showing position pairs where modification
co-occurrence has changed between two conditions, based on the ratio of
odds ratios (ROR). Chords are colored by direction: gained dependencies
(positive log ROR) vs. lost dependencies (negative log ROR).
}
\examples{
\donttest{
results <- read_pipeline_results(
  clover_example("ecoli/config.yaml"),
  types = "odds_ratios"
)
or_data <- results$odds_ratios
or_data$condition <- ifelse(
  grepl("ctl", or_data$sample_id), "ctl", "inf"
)
ror <- compute_ror(or_data, numerator = "inf", denominator = "ctl")
plot_chord_ror(ror)
}
}
