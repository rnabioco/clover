% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-bcerror.R
\name{compute_bcerror_delta}
\alias{compute_bcerror_delta}
\title{Compute per-position delta between two conditions.}
\usage{
compute_bcerror_delta(
  data,
  delta,
  value_col = "mean_error",
  condition_col = "condition"
)
}
\arguments{
\item{data}{A summarized bcerror tibble with at least \code{ref}, \code{pos},
and columns named by \code{value_col} and \code{condition_col}.}

\item{delta}{A bare expression of the form \code{lhs - rhs}, where \code{lhs}
and \code{rhs} are condition levels. The result is \code{lhs - rhs}.}

\item{value_col}{Column name (string) containing the values to
pivot. Default \code{"mean_error"}.}

\item{condition_col}{Column name (string) containing condition
labels. Default \code{"condition"}.}
}
\value{
A tibble with columns \code{ref}, \code{pos}, one column per condition
level, and \code{delta} (the computed difference).
}
\description{
Take a summarized bcerror tibble (with columns for reference, position,
condition, and a value such as mean error rate) and compute the
difference between two conditions at each position.
}
\examples{
df <- tidyr::expand_grid(
  ref = c("tRNA-Ala", "tRNA-Gly"),
  pos = 1:5,
  condition = c("wt", "mut")
)
df$mean_error <- runif(nrow(df), 0, 0.3)
compute_bcerror_delta(df, delta = wt - mut)
}
