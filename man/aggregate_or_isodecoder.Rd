% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odds-ratios.R
\name{aggregate_or_isodecoder}
\alias{aggregate_or_isodecoder}
\title{Aggregate odds ratios to isodecoder level.}
\usage{
aggregate_or_isodecoder(data, pattern = "-\\\\d+-\\\\d+$")
}
\arguments{
\item{data}{A tibble with columns \code{ref}, \code{pos1}, \code{pos2},
\code{log_or_clean} (from \code{\link[=clean_odds_ratios]{clean_odds_ratios()}}), \code{p_value}, and
\code{total_obs}.}

\item{pattern}{Regular expression used to strip gene copy numbers
from \code{ref}. Default \code{"-\\\\d+-\\\\d+$"} matches trailing
\verb{-<copy>-<gene>} suffixes.}
}
\value{
A tibble with columns: \code{isodecoder}, \code{pos1}, \code{pos2},
\code{mean_or}, \code{mean_log_or}, \code{sd_log_or}, \code{min_pval}, \code{total_reads},
and \code{n_copies}.
}
\description{
Collapse per-gene odds ratios to isodecoder level by removing gene
copy numbers from the reference name and summarizing across copies.
}
\examples{
df <- tibble::tibble(
  ref = c("tRNA-Ala-AGC-1-1", "tRNA-Ala-AGC-2-1"),
  pos1 = c(20, 20),
  pos2 = c(34, 34),
  odds_ratio = c(2.5, 3.0),
  log_or_clean = c(0.92, 1.10),
  p_value = c(0.001, 0.01),
  total_obs = c(200, 150)
)
aggregate_or_isodecoder(df)
}
