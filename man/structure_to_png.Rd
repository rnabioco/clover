% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-structure.R
\name{structure_to_png}
\alias{structure_to_png}
\title{Convert a tRNA structure SVG to PNG}
\usage{
structure_to_png(svg_path, output = NULL, width = NULL, height = NULL)
}
\arguments{
\item{svg_path}{Path to an SVG file, typically the return value of
\code{\link[=plot_tRNA_structure]{plot_tRNA_structure()}}.}

\item{output}{Path for the output PNG file. If \code{NULL} (default),
replaces the \code{.svg} extension with \code{.png}.}

\item{width}{Width of the output PNG in pixels. If \code{NULL}
(default), uses the intrinsic SVG width.}

\item{height}{Height of the output PNG in pixels. If \code{NULL}
(default), uses the intrinsic SVG height.}
}
\value{
The path to the PNG file (invisibly).
}
\description{
Renders an SVG file (typically produced by \code{\link[=plot_tRNA_structure]{plot_tRNA_structure()}})
to a PNG bitmap. Requires the
\href{https://cran.r-project.org/package=rsvg}{rsvg} package.
}
\examples{
\dontrun{
svg <- plot_tRNA_structure("tRNA-Glu-TTC", "Escherichia coli")
png <- structure_to_png(svg)
}
}
