% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_bcerror_profile}
\alias{plot_bcerror_profile}
\title{Plot per-position base-calling error profiles.}
\usage{
plot_bcerror_profile(
  data,
  refs = NULL,
  mods = NULL,
  colors = c(ctl = "#0072B2", inf = "#D55E00"),
  ncol = 1
)
}
\arguments{
\item{data}{A summarized bcerror tibble with columns \code{ref}, \code{pos},
\code{condition}, and \code{mean_error}.}

\item{refs}{Character vector of tRNA names to plot (filters
\code{data$ref}). If \code{NULL} (default), all tRNAs are plotted.}

\item{mods}{Optional tibble with \code{ref} and \code{pos} columns marking
modification positions (e.g., from \code{\link[=fetch_modomics_mods]{fetch_modomics_mods()}}).}

\item{colors}{Named character vector of colors for conditions.
Default \code{c(ctl = "#0072B2", inf = "#D55E00")}.}

\item{ncol}{Number of columns for \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}. Default
\code{1}.}
}
\value{
A ggplot object.
}
\description{
Create a line plot of per-position base-calling error rates, faceted
by tRNA and colored by condition. Optionally overlay vertical dashed
lines at known modification positions.
}
\examples{
df <- tidyr::expand_grid(
  ref = c("tRNA-Ala", "tRNA-Gly"),
  pos = 1:20,
  condition = c("ctl", "inf")
)
df$mean_error <- runif(nrow(df), 0, 0.3)
plot_bcerror_profile(df)
}
