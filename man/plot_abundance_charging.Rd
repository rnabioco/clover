% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_abundance_charging}
\alias{plot_abundance_charging}
\title{Plot abundance changes versus charging ratio changes.}
\usage{
plot_abundance_charging(
  deseq_res,
  charging_diffs,
  lab_col = "ref",
  padj_cutoff = 0.05,
  max_overlaps = 20,
  point_size = 2,
  label_size = 3,
  shorten = TRUE,
  source_col = NULL,
  error_bars = TRUE
)
}
\arguments{
\item{deseq_res}{A tibble from \code{\link[=tidy_deseq_results]{tidy_deseq_results()}} with at least
\code{ref}, \code{log2FoldChange}, and \code{padj} columns. When \code{error_bars} is
\code{TRUE}, an \code{lfcSE} column is also expected.}

\item{charging_diffs}{A tibble from \code{\link[=compute_charging_diffs]{compute_charging_diffs()}} with
at least \code{ref} and \code{diff} columns.}

\item{lab_col}{Column name (string) used for point labels. Default
\code{"ref"}.}

\item{padj_cutoff}{Numeric; significance threshold for \code{padj}.
Default \code{0.05}.}

\item{max_overlaps}{Maximum number of overlapping labels passed to
\code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}. Default \code{20}.}

\item{point_size}{Numeric size for \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}. Default
\code{2}.}

\item{label_size}{Numeric size for \code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}.
Default \code{3}.}

\item{shorten}{Logical; if \code{TRUE} (default), shorten tRNA names in
point labels via \code{\link[=shorten_trna_names]{shorten_trna_names()}}.}

\item{source_col}{Optional column name (string) for faceting, e.g.,
\code{"source"} to separate host and phage tRNAs. When provided, the
column must exist in \code{deseq_res}. Default \code{NULL}.}

\item{error_bars}{Logical; if \code{TRUE} (default), draw horizontal
error bars (\code{lfcSE}) on significant points. Requires an \code{lfcSE}
column in \code{deseq_res}.}
}
\value{
A ggplot object.
}
\description{
Creates a scatter plot comparing tRNA abundance changes (from DESeq2)
with charging ratio changes on a single plot. Significant points are
colored by quadrant and labeled with
\code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}.
}
\examples{
deseq_res <- tibble::tibble(
  ref = paste0("tRNA-", 1:6),
  log2FoldChange = c(1, -1, 0.5, -0.5, 2, -2),
  padj = c(0.01, 0.02, 0.5, 0.6, 0.001, 0.003)
)
charging_diffs <- tibble::tibble(
  ref = paste0("tRNA-", 1:6),
  diff = c(0.1, -0.1, 0.05, -0.05, -0.2, 0.15),
  se_diff = rep(0.03, 6)
)
plot_abundance_charging(deseq_res, charging_diffs)
}
