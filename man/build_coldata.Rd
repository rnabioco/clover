% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deseq.R
\name{build_coldata}
\alias{build_coldata}
\title{Build column metadata for DESeq2.}
\usage{
build_coldata(count_matrix, sample_info = NULL)
}
\arguments{
\item{count_matrix}{A count matrix from \code{\link[=abundance_count_matrix]{abundance_count_matrix()}} or
\code{\link[=charging_count_matrix]{charging_count_matrix()}}.}

\item{sample_info}{An optional data frame with a \code{sample_id} column and
additional experimental factor columns (e.g., \code{condition}, \code{replicate}).
If \code{NULL}, a minimal data frame is created from column names.}
}
\value{
A data frame with row names matching \code{colnames(count_matrix)}.
}
\description{
Create a \code{data.frame} of sample metadata suitable for
\code{\link[DESeq2:DESeqDataSet]{DESeq2::DESeqDataSetFromMatrix()}}. For charging count matrices,
the \code{charge_status} factor is added automatically.
}
\examples{
\dontrun{
mat <- abundance_count_matrix(charging)
sample_info <- data.frame(
  sample_id = c("wt_1", "wt_2", "mut_1", "mut_2"),
  condition = c("wt", "wt", "mut", "mut")
)
coldata <- build_coldata(mat, sample_info)
}
}
