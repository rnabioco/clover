% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charging-odds-ratios.R
\name{charging_odds_ratios_from_counts}
\alias{charging_odds_ratios_from_counts}
\title{Compute charging odds ratios from per-site counts.}
\usage{
charging_odds_ratios_from_counts(
  counts,
  min_reads = 10,
  min_margin = 1,
  max_p = 1,
  p_method = "BH"
)
}
\arguments{
\item{counts}{Per-site counts, either a path to a
\verb{\{sample\}.charging_error.tsv.gz} file or a tibble. Requires columns \code{ref},
\code{pos}, \code{err_charged}, \code{err_uncharged}, \code{match_charged} and
\code{match_uncharged}.}

\item{min_reads}{Minimum reads at a site for it to be tested. Default \code{10}.}

\item{min_margin}{Minimum count in each margin of the 2x2 table. Default \code{1}.}

\item{max_p}{Skip sites whose margins put this p-value out of reach. Default
\code{1}, which tests everything.}

\item{p_method}{Method for p-value adjustment, passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}.
Default \code{"BH"}.}
}
\value{
A tibble with the same columns as \code{\link[=compute_charging_odds_ratios]{compute_charging_odds_ratios()}}.
}
\description{
Test the same modification-against-charging association as
\code{\link[=compute_charging_odds_ratios]{compute_charging_odds_ratios()}}, but from a table that already holds each
site's 2x2 counts rather than from per-read calls.
}
\details{
The aa-tRNA-seq pipeline accumulates these counts during the BAM walk it
performs anyway, writing \verb{\{sample\}.charging_error.tsv.gz} with one row per
site instead of one row per read and position. That is the same information
as far as this test is concerned -- the statistics are identical -- and it
turns a table of tens of millions of rows into tens of thousands, so prefer
this entry point unless you need read-level flexibility such as
re-thresholding the charging call or joining other per-read features.

The caveats on \code{\link[=compute_charging_odds_ratios]{compute_charging_odds_ratios()}} apply unchanged, in
particular that sites near the 3' end are not interpretable.
}
\examples{
counts <- tibble::tibble(
  ref = "tRNA-Ala-AGC-1-1",
  pos = c(34L, 58L),
  err_charged = c(120, 40),
  err_uncharged = c(30, 45),
  match_charged = c(200, 280),
  match_uncharged = c(400, 385)
)
charging_odds_ratios_from_counts(counts)
}
\seealso{
\code{\link[=compute_charging_odds_ratios]{compute_charging_odds_ratios()}}
}
