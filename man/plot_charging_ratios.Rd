% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_charging_ratios}
\alias{plot_charging_ratios}
\title{Plot per-tRNA charging ratios.}
\usage{
plot_charging_ratios(
  data,
  group_col = "condition",
  facet_col = NULL,
  shorten = TRUE,
  point_size = 0.8,
  point_alpha = 0.4
)
}
\arguments{
\item{data}{A tibble with at least \code{ref}, \code{charging_ratio}, and the
column named by \code{group_col}.}

\item{group_col}{Column name (string) for the x-axis grouping
variable (e.g., \code{"condition"}). Default \code{"condition"}.}

\item{facet_col}{Optional column name (string) for faceting (e.g.,
\code{"strain"}). Default \code{NULL}.}

\item{shorten}{Logical; if \code{TRUE} (default), shorten tRNA names on
the y-axis via \code{\link[=shorten_trna_names]{shorten_trna_names()}}.}

\item{point_size}{Numeric size for \code{\link[ggplot2:geom_jitter]{ggplot2::geom_jitter()}}. Default
\code{0.8}.}

\item{point_alpha}{Numeric alpha for jittered points. Default \code{0.4}.}
}
\value{
A ggplot object.
}
\description{
Create a box-and-jitter plot of raw charging ratios grouped by
condition. Expects the tibble stored in
\code{metadata(se)$charging_ratios}.
}
\examples{
df <- tibble::tibble(
  ref = rep(paste0("tRNA-Ala-AGC-", 1:3, "-1"), each = 6),
  condition = rep(c("ctl", "inf"), each = 3, times = 3),
  charging_ratio = runif(18, 0.3, 0.9)
)
plot_charging_ratios(df)
}
