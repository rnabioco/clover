% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_mod_heatmap}
\alias{plot_mod_heatmap}
\title{Plot a delta-signal modification heatmap.}
\usage{
plot_mod_heatmap(
  data,
  value_col = "value",
  ref_col = "ref",
  cluster = TRUE,
  color_limits = c(-0.25, 0.25),
  color_low = "#0072B2",
  color_high = "#D55E00",
  na_value = "gray80",
  square = TRUE
)
}
\arguments{
\item{data}{A data frame with at least three columns: one for tRNA
family/reference (y-axis), one for Sprinzl position labels (x-axis),
and one for the fill value.}

\item{value_col}{Column name (string) for fill values. Default \code{"value"}.}

\item{ref_col}{Column name (string) for tRNA families (y-axis). Default
\code{"ref"}.}

\item{cluster}{Logical; cluster rows with Ward's D2? Default \code{TRUE}.}

\item{color_limits}{Numeric vector of length 2 giving symmetric limits
for the color scale. Default \code{c(-0.25, 0.25)}.}

\item{color_low}{Color for negative values. Default \code{"#0072B2"} (blue).}

\item{color_high}{Color for positive values. Default \code{"#D55E00"} (red).}

\item{na_value}{Color for missing positions. Default \code{"gray80"}.}

\item{square}{Logical; use \code{coord_fixed(ratio = 1)}? Default \code{TRUE}.}
}
\value{
A ggplot object.
}
\description{
Create a diverging heatmap of modification signal changes (e.g., mutant
minus wild-type) across tRNA families and Sprinzl positions. Rows can be
optionally clustered using Ward's D2 hierarchical clustering.
}
\examples{
df <- tidyr::expand_grid(
  ref = paste0("tRNA-", c("Ala", "Gly", "Ser")),
  sprinzl_label = as.character(1:10)
)
df$value <- rnorm(nrow(df), sd = 0.1)
plot_mod_heatmap(df)
}
