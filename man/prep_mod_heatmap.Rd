% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{prep_mod_heatmap}
\alias{prep_mod_heatmap}
\title{Prepare data for \code{\link[=plot_mod_heatmap]{plot_mod_heatmap()}}.}
\usage{
prep_mod_heatmap(
  data,
  value_col = "delta",
  ref_col = "ref",
  sprinzl_coords,
  mods = NULL,
  strip_prefix = "^host-",
  shorten_labels = TRUE
)
}
\arguments{
\item{data}{A tibble with at least \code{ref}, \code{pos}, and a value column
(e.g., \code{delta} from \code{\link[=compute_bcerror_delta]{compute_bcerror_delta()}}).}

\item{value_col}{Column name (string) for the fill value. Default
\code{"delta"}.}

\item{ref_col}{Column name (string) for the tRNA reference. Default
\code{"ref"}.}

\item{sprinzl_coords}{A tibble of Sprinzl coordinates from
\code{\link[=read_sprinzl_coords]{read_sprinzl_coords()}}, with at least \code{trna_id}, \code{pos},
\code{sprinzl_label}, and \code{global_index} columns.}

\item{mods}{Optional tibble of modification annotations (e.g., from
\code{\link[=modomics_mods]{modomics_mods()}}) with at least \code{ref} and \code{pos} columns. When
provided, a logical \code{has_mod} column is added.}

\item{strip_prefix}{Regex pattern to strip from \code{ref} before
matching Sprinzl coordinates. Default \code{"^host-"}.}

\item{shorten_labels}{Logical; if \code{TRUE} (default), create a
\code{trna_label} column with shortened names (e.g.,
\code{"tRNA-Glu-TTC-1-1"} becomes \code{"Glu-TTC"}).}
}
\value{
A tibble ready for \code{\link[=plot_mod_heatmap]{plot_mod_heatmap()}}, with
\code{sprinzl_label} as an ordered factor and optionally \code{has_mod} and
\code{trna_label} columns.
}
\description{
Join Sprinzl coordinates to a bcerror delta tibble, optionally
annotate known modifications, order positions by canonical Sprinzl
index, and shorten tRNA names for display.
}
\examples{
\dontrun{
bcerror_delta <- compute_bcerror_delta(bcerror_summary, delta = wt - tb)
sprinzl <- read_sprinzl_coords(
  clover_example("sprinzl/ecoliK12_global_coords.tsv.gz")
)
mods <- modomics_mods(trna_fasta, organism = "Escherichia coli")
heatmap_data <- prep_mod_heatmap(
  bcerror_delta,
  sprinzl_coords = sprinzl,
  mods = mods
)
}
}
