% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clover-se.R
\name{create_clover}
\alias{create_clover}
\title{Create a SummarizedExperiment from pipeline output.}
\usage{
create_clover(
  config_path,
  types = c("charging", "bcerror", "odds_ratios"),
  sample_info = NULL,
  min_count = 10
)
}
\arguments{
\item{config_path}{Path to a pipeline \code{config.yaml} file.}

\item{types}{Character vector of result types to load. Valid values:
\code{"charging"}, \code{"bcerror"}, \code{"odds_ratios"}.}

\item{sample_info}{An optional data frame with a \code{sample_id} column and
additional experimental factor columns (e.g., \code{condition}, \code{replicate}).
If \code{NULL}, a minimal colData is created from sample names.}

\item{min_count}{Minimum total count across all samples for a tRNA
to be retained in count matrices. Default \code{10}.}
}
\value{
A \link[SummarizedExperiment:SummarizedExperiment-class]{SummarizedExperiment::SummarizedExperiment} with:
\itemize{
\item \strong{assay "counts"}: abundance count matrix (charged + uncharged)
\item \strong{colData}: sample metadata
\item \strong{metadata}: list with \verb{$config}, \verb{$charging}, \verb{$bcerror},
\verb{$odds_ratios}, \verb{$fasta} as available
}
}
\description{
Read a pipeline configuration file and load all result types into a
\link[SummarizedExperiment:SummarizedExperiment-class]{SummarizedExperiment::SummarizedExperiment} object. The returned object
contains count matrices as assays, tRNA metadata as rowData, and
sample metadata as colData.
}
\examples{
se <- create_clover(clover_example("ecoli/config.yaml"))
SummarizedExperiment::assay(se, "counts")[1:3, ]
SummarizedExperiment::colData(se)
}
