% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identity.R
\name{identity_elements}
\alias{identity_elements}
\title{Retrieve tRNA aminoacylation identity elements}
\usage{
identity_elements(
  organism,
  amino_acid = NULL,
  type = c("both", "determinant", "antideterminant")
)
}
\arguments{
\item{organism}{Character string specifying the organism name
(e.g., \code{"Escherichia coli"}, \code{"Saccharomyces cerevisiae"},
\code{"Homo sapiens"}). Use \code{\link[=identity_organisms]{identity_organisms()}} to list supported
organisms.}

\item{amino_acid}{Optional character vector of 3-letter amino acid
codes to filter by (e.g., \code{"Ala"}, \code{"Arg"}). If \code{NULL} (default),
returns elements for all amino acids.}

\item{type}{Which element types to return: \code{"both"} (default),
\code{"determinant"}, or \code{"antideterminant"}.}
}
\value{
A tibble with columns:
\itemize{
\item \code{amino_acid}: 3-letter amino acid code
\item \code{domain}: evolutionary domain (\code{"Bacteria"} or \code{"Eukarya"})
\item \code{sprinzl_pos}: Sprinzl position number (integer; \code{NA} for
structural determinants like the long variable arm; \code{-1} for
the His G\eqn{_{-1}} position)
\item \code{nucleotide}: expected base (A/C/G/U, or \code{NA} if variable)
\item \code{region}: tRNA structural region name
\item \code{type}: \code{"determinant"} or \code{"antideterminant"}
\item \code{strength}: \code{"strong"} or \code{"weak"} (determinants only)
\item \code{pair_pos}: partner Sprinzl position for base pairs (\code{NA}
for single-nucleotide elements)
\item \code{pair_type}: \code{"wc"} or \code{"wobble"} (\code{NA} for unpaired)
\item \code{against_aars}: aaRS blocked (antideterminants only)
\item \code{universal}: \code{TRUE} if conserved across all domains
\item \code{description}: human-readable description
}
}
\description{
Returns experimentally validated identity elements (determinants
and antideterminants) for tRNA aminoacylation, based on data from
Giege & Eriani (2023). Elements are defined per amino acid family
using Sprinzl numbering.
}
\section{Limitations}{

Selenocysteine tRNA (SeC, anticodon UCA) is not currently
supported. Its non-canonical 90-nt structure with an extended
variable arm is not represented in the bundled determinant or
antideterminant tables, and Sprinzl coordinates for SeC tRNAs are
likewise unavailable.
}

\examples{
# All identity elements for E. coli
identity_elements("Escherichia coli")

# Ala determinants only
identity_elements("Escherichia coli", amino_acid = "Ala",
  type = "determinant")
}
\references{
Giege R, Eriani G (2023). "The tRNA identity landscape for
aminoacylation and beyond." \emph{Nucleic Acids Research}, 51(4),
1528--1570. \doi{10.1093/nar/gkad007}
}
