% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deseq.R
\name{abundance_count_matrix}
\alias{abundance_count_matrix}
\title{Build a tRNA abundance count matrix from charging data.}
\usage{
abundance_count_matrix(charging_data, min_count = 10)
}
\arguments{
\item{charging_data}{A tibble of combined charging data with columns
\code{ref}, \code{counts_charged}, \code{counts_uncharged}, and \code{sample_id}
(as returned by \code{\link[=read_charging_multi]{read_charging_multi()}}).}

\item{min_count}{Minimum total count across all samples for a tRNA
to be retained. Default \code{10}.}
}
\value{
An integer matrix with tRNA names as row names and sample IDs
as column names.
}
\description{
Pivot charging data into an integer matrix suitable for DESeq2, where
each row is a tRNA and each column is a sample. Values are total
abundance (charged + uncharged counts).
}
\examples{
results <- read_pipeline_results(
  clover_example("ecoli/config.yaml"),
  types = "charging"
)
abundance_count_matrix(results$charging)
}
