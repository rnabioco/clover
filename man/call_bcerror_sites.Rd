% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charging-odds-ratios.R
\name{call_bcerror_sites}
\alias{call_bcerror_sites}
\title{Call modified sites from base-calling error.}
\usage{
call_bcerror_sites(bcerror, min_error = 0.1, min_cov = 20, refs = NULL)
}
\arguments{
\item{bcerror}{A bcerror tibble from \code{\link[=read_bcerror]{read_bcerror()}}, with columns \code{ref},
\code{pos}, \code{cov}, and \code{error_rate}.}

\item{min_error}{Minimum \code{error_rate} for a position to be called. Default
\code{0.1}.}

\item{min_cov}{Minimum coverage (\code{cov}) for a position to be considered.
Default \code{20}.}

\item{refs}{Optional character vector of reference names to restrict to.
If \code{NULL} (default), all references are considered.}
}
\value{
A tibble with columns \code{ref}, \code{pos}, \code{cov}, and \code{error_rate}, sorted
by reference and position.
}
\description{
Select candidate modification sites from a bcerror tibble by thresholding
the base-calling error rate. The result is a site list suitable for the
\code{sites} argument of \code{\link[=compute_charging_odds_ratios]{compute_charging_odds_ratios()}} and
\code{\link[=compute_odds_ratios]{compute_odds_ratios()}}.
}
\details{
This is an alternative to selecting sites from direct modification calls:
base-calling error is an orthogonal, model-free signal, so it can be used
when the modification-caller channels are not trusted for a given dataset.
Note that error-derived sites carry no modification identity — a site is
called because reads misbehave there, not because a particular modification
was identified.
}
\examples{
bcerr_path <- clover_example(
  "ecoli/summary/tables/wt-15-ctl-01/wt-15-ctl-01.bcerror.tsv.gz"
)
bcerr <- read_bcerror(bcerr_path)
call_bcerror_sites(bcerr, min_error = 0.05, min_cov = 10)
}
\seealso{
\code{\link[=read_bcerror]{read_bcerror()}}, \code{\link[=compute_charging_odds_ratios]{compute_charging_odds_ratios()}}
}
