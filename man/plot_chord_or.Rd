% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-chord.R
\name{plot_chord_or}
\alias{plot_chord_or}
\title{Plot a chord diagram of modification co-occurrence.}
\usage{
plot_chord_or(
  odds_data,
  or_col = "log_odds_ratio",
  or_cutoff = 0.5,
  p_col = "p_value",
  p_cutoff = 0.05,
  min_obs = 50,
  positive_color = "#D55E00",
  negative_color = "#0072B2",
  sprinzl_coords = NULL,
  title = NULL,
  transparency = 0.4
)
}
\arguments{
\item{odds_data}{A tibble of odds ratio data with columns \code{pos1}, \code{pos2},
\code{log_odds_ratio} (or column specified by \code{or_col}), \code{p_value} (or column
specified by \code{p_col}), and \code{total_obs}.}

\item{or_col}{Column name (string) for the odds ratio value.
Default \code{"log_odds_ratio"}.}

\item{or_cutoff}{Minimum absolute value of \code{or_col} for a chord to be
drawn. Default \code{0.5}.}

\item{p_col}{Column name (string) for the p-value. Default \code{"p_value"}.}

\item{p_cutoff}{Maximum p-value for a chord to be drawn. Default \code{0.05}.}

\item{min_obs}{Minimum number of observations (\code{total_obs}) for a pair
to be included. Default \code{50}.}

\item{positive_color}{Color for positive odds ratios (co-occurrence).
Default \code{"#D55E00"} (vermillion).}

\item{negative_color}{Color for negative odds ratios (exclusion).
Default \code{"#0072B2"} (blue).}

\item{sprinzl_coords}{An optional tibble from \code{\link[=read_sprinzl_coords]{read_sprinzl_coords()}} used
to order sectors by Sprinzl position and color by structural region.}

\item{title}{Optional plot title.}

\item{transparency}{Transparency for chord colors (0 = opaque, 1 = fully
transparent). Default \code{0.4}.}
}
\value{
\code{invisible(NULL)}. Called for its side effect of producing a
base graphics plot.
}
\description{
Display a circlize chord diagram showing significant pairwise modification
co-occurrence (odds ratios) for a single sample. Chords are colored by
direction: positive odds ratios (co-occurring modifications) vs. negative
(mutually exclusive). Chord width reflects the magnitude of the log odds
ratio.
}
\examples{
\dontrun{
or_data <- read_odds_ratios("sample1.odds_ratios.tsv.gz")
plot_chord_or(or_data)
}
}
