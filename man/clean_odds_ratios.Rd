% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odds-ratios.R
\name{clean_odds_ratios}
\alias{clean_odds_ratios}
\title{Clean odds ratio data.}
\usage{
clean_odds_ratios(data, cap_inf = 2)
}
\arguments{
\item{data}{A tibble of odds ratio data with columns \code{ref},
\code{odds_ratio}, and \code{log_odds_ratio}.}

\item{cap_inf}{Numeric value added to (or subtracted from) the
finite range when capping positive (or negative) infinite log
odds ratios. Default \code{2}.}
}
\value{
A tibble with a new \code{log_or_clean} column where infinite
values have been capped.
}
\description{
Prepare odds ratio data for downstream analysis by capping infinite
log odds ratio values and optionally removing adapter positions.
}
\examples{
df <- tibble::tibble(
  ref = "tRNA-Ala-AGC-1-1",
  pos1 = c(20, 34),
  pos2 = c(34, 58),
  odds_ratio = c(2.5, Inf),
  log_odds_ratio = c(0.92, Inf),
  p_value = c(0.001, 0.01),
  total_obs = c(200, 150)
)
clean_odds_ratios(df)
}
