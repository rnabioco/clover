% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rewiring.R
\name{prepare_rewiring_matrix}
\alias{prepare_rewiring_matrix}
\title{Prepare a matrix for rewiring PCoA analysis.}
\usage{
prepare_rewiring_matrix(data, sig_only = TRUE, value_cap = 10)
}
\arguments{
\item{data}{A tibble with columns \code{isodecoder}, \code{pos1}, \code{pos2}, and
\code{ror}. Typically the output of \code{\link[=compute_ror_isodecoder]{compute_ror_isodecoder()}}.}

\item{sig_only}{Logical; filter to rows where \code{significant} is
\code{TRUE}? Default \code{TRUE}.}

\item{value_cap}{Maximum absolute ROR value. Values beyond this are
capped. Default \code{10}.}
}
\value{
A numeric matrix with isodecoders as row names and position
pair labels (\code{pos1_vs_pos2}) as column names.
}
\description{
Build a wide matrix from isodecoder-level relative odds ratios
suitable for PCoA or distance calculations. Rows are isodecoders,
columns are position pair comparisons.
}
\examples{
df <- tibble::tibble(
  isodecoder = rep(c("tRNA-Ala", "tRNA-Gly"), each = 2),
  pos1 = c(20, 34, 20, 34),
  pos2 = c(34, 58, 34, 58),
  ror = c(1.5, -0.8, 0.3, 2.1),
  significant = c(TRUE, TRUE, TRUE, TRUE)
)
prepare_rewiring_matrix(df)
}
