% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_volcano}
\alias{plot_volcano}
\title{Plot a volcano plot of differential expression results.}
\usage{
plot_volcano(
  data,
  lab_col = "ref",
  padj_cutoff = 0.05,
  max_overlaps = 20,
  point_size = 1.5,
  label_size = 3,
  sig_color = "#D55E00",
  nonsig_color = "grey60"
)
}
\arguments{
\item{data}{A tibble from \code{\link[=tidy_deseq_results]{tidy_deseq_results()}} with at least
\code{log2FoldChange}, \code{padj}, and \code{significant} columns.}

\item{lab_col}{Column name (string) used for point labels. Default
\code{"ref"}.}

\item{padj_cutoff}{Numeric; draws a dashed horizontal line at
\code{-log10(padj_cutoff)}. Default \code{0.05}.}

\item{max_overlaps}{Maximum number of overlapping labels passed to
\code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}. Default \code{20}.}

\item{point_size}{Numeric size for \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}. Default
\code{1.5}.}

\item{label_size}{Numeric size for \code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}.
Default \code{3}.}

\item{sig_color}{Color for significant points. Default \code{"#D55E00"}.}

\item{nonsig_color}{Color for non-significant points. Default
\code{"grey60"}.}
}
\value{
A ggplot object.
}
\description{
Creates a volcano plot from the tibble returned by
\code{\link[=tidy_deseq_results]{tidy_deseq_results()}}. Significant points are labeled with
\code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}.
}
\examples{
res <- tibble::tibble(
  ref = paste0("tRNA-", 1:10),
  log2FoldChange = rnorm(10),
  pvalue = c(rep(0.001, 3), rep(0.5, 7)),
  padj = c(rep(0.01, 3), rep(0.8, 7)),
  significant = c(rep(TRUE, 3), rep(FALSE, 7))
)
plot_volcano(res)
}
