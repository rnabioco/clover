% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_pcoa_rewiring}
\alias{plot_pcoa_rewiring}
\title{Plot PCoA of tRNA rewiring scores.}
\usage{
plot_pcoa_rewiring(
  pcoa_result,
  rewiring_scores,
  title = "PCoA of tRNA rewiring",
  n_label = 10
)
}
\arguments{
\item{pcoa_result}{A list from \code{\link[=perform_pcoa]{perform_pcoa()}} with elements
\code{coordinates} and \code{variance_explained}.}

\item{rewiring_scores}{A tibble from \code{\link[=calculate_rewiring_scores]{calculate_rewiring_scores()}}
with at least \code{isodecoder}, \code{euclidean_magnitude}, and
\code{n_nonzero} columns.}

\item{title}{Plot title. Default \code{"PCoA of tRNA rewiring"}.}

\item{n_label}{Number of top isodecoders to label. Default \code{10}.}
}
\value{
A ggplot object.
}
\description{
Create a scatter plot of PCoA coordinates from
\code{\link[=perform_pcoa]{perform_pcoa()}}, with points sized by the number of non-zero
comparisons and colored by Euclidean rewiring magnitude. The top
\code{n_label} isodecoders are labeled with
\code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}.
}
\examples{
\donttest{
mat <- matrix(
  c(1.5, -0.8, 0.3, 2.1, 0.5, -1.2),
  nrow = 3,
  dimnames = list(
    c("tRNA-Ala", "tRNA-Gly", "tRNA-Ser"),
    c("20_vs_34", "34_vs_58")
  )
)
scores <- calculate_rewiring_scores(mat)
pcoa <- perform_pcoa(mat)
plot_pcoa_rewiring(pcoa, scores)
}
}
