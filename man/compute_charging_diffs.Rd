% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-charging.R
\name{compute_charging_diffs}
\alias{compute_charging_diffs}
\title{Compute charging ratio differences between conditions.}
\usage{
compute_charging_diffs(
  charging_data,
  condition_col = "condition",
  numerator,
  denominator,
  min_count = 50,
  n_top = NULL
)
}
\arguments{
\item{charging_data}{A tibble from \code{\link[=read_charging_multi]{read_charging_multi()}} with an
added condition column. Must contain \code{tRNA}, \code{counts_charged},
\code{counts_uncharged}, \code{sample_id}, and the column named by
\code{condition_col}.}

\item{condition_col}{Column name (string) for condition labels.
Default \code{"condition"}.}

\item{numerator}{Value of \code{condition_col} for the numerator
condition.}

\item{denominator}{Value of \code{condition_col} for the denominator
condition.}

\item{min_count}{Minimum total reads (charged + uncharged) per tRNA
per sample to include. Default \code{50}.}

\item{n_top}{If non-\code{NULL}, keep only the top \code{n_top} tRNAs by
total abundance across all samples. Default \code{NULL} (keep all).}
}
\value{
A tibble with columns:
\describe{
\item{tRNA}{tRNA identifier.}
\item{ratio_numerator}{Mean charging ratio for the numerator
condition.}
\item{ratio_denominator}{Mean charging ratio for the denominator
condition.}
\item{se_numerator}{Standard error of the numerator ratio.}
\item{se_denominator}{Standard error of the denominator ratio.}
\item{diff}{Difference: \code{ratio_numerator - ratio_denominator}.}
\item{se_diff}{Propagated SE:
\code{sqrt(se_numerator^2 + se_denominator^2)}.}
}
}
\description{
Compare per-tRNA charging ratios (charged / total) between two
conditions, summarizing replicates with mean and standard error and
computing the between-condition difference with propagated SE.
}
\examples{
\dontrun{
paths <- c(
  ctl1 = "ctl1.charging.cpm.tsv.gz",
  ctl2 = "ctl2.charging.cpm.tsv.gz",
  inf1 = "inf1.charging.cpm.tsv.gz",
  inf2 = "inf2.charging.cpm.tsv.gz"
)
charging <- read_charging_multi(paths)
charging$condition <- ifelse(
  grepl("ctl", charging$sample_id), "ctl", "inf"
)
diffs <- compute_charging_diffs(
  charging,
  numerator = "inf",
  denominator = "ctl"
)
}
}
