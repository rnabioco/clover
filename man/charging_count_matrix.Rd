% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deseq.R
\name{charging_count_matrix}
\alias{charging_count_matrix}
\title{Build a charging count matrix for differential charging analysis.}
\usage{
charging_count_matrix(charging_data, min_count = 10)
}
\arguments{
\item{charging_data}{A tibble of combined charging data with columns
\code{ref}, \code{counts_charged}, \code{counts_uncharged}, and \code{sample_id}
(as returned by \code{\link[=read_charging_multi]{read_charging_multi()}}).}

\item{min_count}{Minimum total count across all columns for a tRNA
to be retained. Default \code{10}.}
}
\value{
An integer matrix with tRNA names as row names. Column names
are \verb{\{sample_id\}_charged} and \verb{\{sample_id\}_uncharged}.
}
\description{
Create a count matrix with two columns per sample (charged and uncharged),
suitable for a DESeq2 interaction design that tests for differences in
charging ratios between conditions.
}
\examples{
\dontrun{
charging <- read_charging_multi(paths)
mat <- charging_count_matrix(charging)
}
}
