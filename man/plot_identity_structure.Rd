% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identity.R
\name{plot_identity_structure}
\alias{plot_identity_structure}
\title{Plot tRNA structure with identity element overlays}
\usage{
plot_identity_structure(
  trna,
  organism,
  sprinzl_coords,
  trna_id = NULL,
  amino_acid = NULL,
  outline_palette = NULL,
  ...
)
}
\arguments{
\item{trna}{Character string identifying the tRNA
(e.g., \code{"tRNA-Ala-AGC"}). Use \code{\link[=structure_trnas]{structure_trnas()}} to list
available tRNAs.}

\item{organism}{Character string specifying the organism name
(e.g., \code{"Saccharomyces cerevisiae"}).}

\item{sprinzl_coords}{A tibble of Sprinzl coordinates as returned
by \code{\link[=read_sprinzl_coords]{read_sprinzl_coords()}}.}

\item{trna_id}{Character string identifying the tRNA in
\code{sprinzl_coords} (e.g., \code{"nuc-tRNA-Ala-AGC-1-1"}). If \code{NULL}
(default), auto-detected from \code{trna} by converting the anticodon
to RNA (T to U) and matching the first Sprinzl entry.}

\item{amino_acid}{Optional 3-letter amino acid code. If \code{NULL}
(default), extracted from the \code{trna} name.}

\item{outline_palette}{Named character vector of colors keyed by
strength (\code{"strong"}, \code{"weak"}). Default uses red for strong
and blue for weak determinants.}

\item{...}{Additional arguments passed to
\code{\link[=plot_tRNA_structure]{plot_tRNA_structure()}}.}
}
\value{
The path to the annotated SVG file (invisibly).
}
\description{
Generates a tRNA cloverleaf structure SVG with aminoacylation
identity elements highlighted as colored outlines. Strong
determinants are shown in red, weak determinants in blue.
}
\examples{
\donttest{
coords <- read_sprinzl_coords(
  clover_example("sprinzl/sacCer_global_coords.tsv.gz")
)
plot_identity_structure(
  "tRNA-Ala-AGC", "Saccharomyces cerevisiae", coords
)
}
}
