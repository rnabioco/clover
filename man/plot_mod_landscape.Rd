% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_mod_landscape}
\alias{plot_mod_landscape}
\title{Plot per-tRNA modification landscape profiles.}
\usage{
plot_mod_landscape(
  data,
  metrics,
  pos_col = "pos",
  region_col = NULL,
  sprinzl_col = NULL,
  mod_col = NULL,
  title = NULL,
  heights = NULL
)
}
\arguments{
\item{data}{A data frame with a position column and one or more
metric columns to plot.}

\item{metrics}{Character vector of column names to plot as stacked
panels (one panel per metric).}

\item{pos_col}{Column name (string) for x-axis positions. Default
\code{"pos"}.}

\item{region_col}{Optional column name (string) for structural
region labels, used for background shading. Default \code{NULL}.}

\item{sprinzl_col}{Optional column name (string) for Sprinzl
position labels shown on a secondary x-axis. Default \code{NULL}.}

\item{mod_col}{Optional column name (string) of a logical column;
\code{TRUE} positions are highlighted on the Sprinzl axis (requires
ggtext). Default \code{NULL}.}

\item{title}{Plot title. Default \code{NULL}.}

\item{heights}{Numeric vector of relative panel heights. Default
\code{NULL} (equal heights).}
}
\value{
A patchwork object combining stacked ggplot panels.
}
\description{
Create a stacked panel plot showing multiple metrics along the tRNA
sequence. Each metric gets its own panel sharing a common x-axis.
Optionally adds structural region background shading and a secondary
x-axis with Sprinzl position labels.
}
\examples{
df <- data.frame(
  pos = rep(1:20, 2),
  condition = rep(c("ctl", "mut"), each = 20),
  error_rate = runif(40, 0, 0.3),
  signal = rnorm(40, sd = 0.1)
)
plot_mod_landscape(df, metrics = c("error_rate", "signal"))
}
