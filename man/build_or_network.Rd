% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-network.R
\name{build_or_network}
\alias{build_or_network}
\title{Build a network from odds ratio data.}
\usage{
build_or_network(data, value_col = "ror", min_weight = 0)
}
\arguments{
\item{data}{A tibble with columns \code{pos1}, \code{pos2}, and a value
column specified by \code{value_col}. Optionally includes \code{p_adj}.}

\item{value_col}{Column name (string) for edge values. Default
\code{"ror"}.}

\item{min_weight}{Minimum absolute value of \code{value_col} for an edge
to be included. Default \code{0}.}
}
\value{
A \code{tbl_graph} object with node metrics \code{degree} and
\code{betweenness}, or \code{NULL} if no edges remain after filtering.
}
\description{
Construct a tidygraph network from pairwise odds ratio or ROR data.
Each node is a position (or isodecoder) and each edge represents a
significant co-occurrence relationship. Requires the tidygraph and
igraph packages.
}
\examples{
\donttest{
df <- tibble::tibble(
  pos1 = c("20", "34", "20"),
  pos2 = c("34", "58", "58"),
  ror = c(1.5, -0.8, 0.3)
)
build_or_network(df)
}
}
