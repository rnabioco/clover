% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odds-ratios.R
\name{compute_ror}
\alias{compute_ror}
\title{Compute ratio of odds ratios between conditions.}
\usage{
compute_ror(
  odds_data,
  condition_col = "condition",
  numerator,
  denominator,
  min_obs = 100,
  agg_fun = mean
)
}
\arguments{
\item{odds_data}{A combined tibble of odds ratio data with a \code{condition}
column (or column specified by \code{condition_col}) and \code{sample_id}.}

\item{condition_col}{Column name (string) for condition labels.
Default \code{"condition"}.}

\item{numerator}{Value of \code{condition_col} for the numerator condition.}

\item{denominator}{Value of \code{condition_col} for the denominator condition.}

\item{min_obs}{Minimum \code{total_obs} for a pair to be included.
Default \code{100}.}

\item{agg_fun}{Function to aggregate replicate log odds ratios.
Default \code{mean}.}
}
\value{
A tibble with columns: \code{pos1}, \code{pos2}, \code{or_numerator},
\code{or_denominator}, \code{ror}, and \code{log_ror}.
}
\description{
Compare modification co-occurrence between two conditions by computing
the ratio of odds ratios (ROR). Replicates within each condition are
aggregated using the specified function.
}
\examples{
results <- read_pipeline_results(
  clover_example("ecoli/config.yaml"),
  types = "odds_ratios"
)
or_data <- results$odds_ratios
or_data$condition <- ifelse(
  grepl("ctl", or_data$sample_id), "ctl", "inf"
)
compute_ror(or_data, numerator = "inf", denominator = "ctl")
}
