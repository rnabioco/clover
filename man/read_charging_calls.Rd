% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charging-odds-ratios.R
\name{read_charging_calls}
\alias{read_charging_calls}
\title{Read per-read charging calls.}
\usage{
read_charging_calls(path, ml_threshold = 200)
}
\arguments{
\item{path}{Path to a \verb{\{sample\}.charging_prob.tsv.gz} file, with columns
\code{read_id}, \code{tRNA}, and \code{charging_likelihood}.}

\item{ml_threshold}{Minimum \code{charging_likelihood} (the \code{CL} tag) for a read
to be called charged. Default \code{200}, matching the pipeline default.}
}
\value{
A tibble with columns \code{read_id}, \code{ref}, and \code{charged} (0/1).
}
\description{
Read a per-read charging likelihood file produced by the tRNA sequencing
pipeline and binarize it at \code{ml_threshold}. Unlike \code{\link[=read_charging]{read_charging()}}, which
returns per-tRNA aggregate counts, this returns one row per read and is the
input required by \code{\link[=compute_charging_odds_ratios]{compute_charging_odds_ratios()}}.
}
\examples{
path <- clover_example("ecoli/charging_calls.tsv.gz")
read_charging_calls(path)
}
\seealso{
\code{\link[=compute_charging_odds_ratios]{compute_charging_odds_ratios()}}, \code{\link[=read_charging]{read_charging()}}
}
